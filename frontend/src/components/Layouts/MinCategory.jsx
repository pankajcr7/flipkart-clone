import ExpandMoreIcon from '@mui/icons-material/ExpandMore';
import { Link } from 'react-router-dom';

const categories = [
    // Navigation categories removed as requested
]

const MinCategory = () => {
    return (
        <>
            {/* Navigation categories removed as requested */}
        </>
    );
};

export default MinCategory;
